import { defineMessages } from 'react-intl';

const messages = defineMessages({
  removeAll: {
    id: 'app.itemContainer.removeAll',
    defaultMessage: 'Remove all',
  },
});

export default messages;
